const selectList = ({ list }) => list

const selectCart = ({ cart }) => cart

const selectModal = ({showModal}) => showModal

const selectors = {
    selectList,
    selectCart,
    selectModal
}

export default selectors